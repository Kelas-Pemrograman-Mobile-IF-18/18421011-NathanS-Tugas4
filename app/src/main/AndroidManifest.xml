<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nathans.project30">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sadicon"
        android:label="Pindah Halaman"
        android:roundIcon="@mipmap/sadicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project30">
        <activity android:name=".Halaman2"></activity>
        <activity android:name=".Halaman1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>